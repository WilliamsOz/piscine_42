Assignment name  : sort_int_tab
Expected files   : sort_int_tab.c
Allowed functions: 
--------------------------------------------------------------------------------

Write the following function:

void sort_int_tab(int *tab, unsigned int size);

It must sort (in-place) the 'tab' int array, that contains exactly 'size'
members, in ascending order.

Doubles must be preserved.

Input is always coherent.

Écrivez la fonction suivante:

void sort_int_tab (onglet int *, taille int non signée);

Il doit trier (sur place) le tableau int 'tab', qui contient exactement 'size'
membres, par ordre croissant.

Les doubles doivent être préservés.

Lentrée est toujours cohérente.