Assignment name  : pgcd
Expected files   : pgcd.c
Allowed functions: printf, atoi, malloc, free
--------------------------------------------------------------------------------

Write a program that takes two strings representing two strictly positive
integers that fit in an int.

Display their highest common denominator followed by a newline (It iss always a
strictly positive integer).

If the number of parameters is not 2, display a newline.

Écrire un programme qui prend deux chaînes représentant deux entiers strictement
positifs qui correspondent à un int.

Afficher leur dénominateur commun le plus élevé suivi dune nouvelle ligne (cest toujours un
entier strictement positif).

Si le nombre de paramètres nest pas 2, affichez une nouvelle ligne.

Exemples:

$> ./pgcd 42 10 | cat -e
2$
$> ./pgcd 42 12 | cat -e
6$
$> ./pgcd 14 77 | cat -e
7$
$> ./pgcd 17 3 | cat -e 
1$
$> ./pgcd | cat -e
$
